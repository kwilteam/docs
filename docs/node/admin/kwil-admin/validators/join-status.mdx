---
sidebar_position: 3
sidebar_label: "join-status"
id: "kwil-admin-validators-join-status"
title: "kwil-admin validators join-status"
description: "Query the status of a pending validator join request."
---

Query the status of a pending validator join request.

### Usage

```
kwil-admin validators join-status <joiner> [flags]
```

### Example

```
# Query the status of a pending validator join request, by hex public key
kwil-admin validators join-status 6ecaca8e9394c939a858c2c7b47acb1db26a96d7ab38bd702fa3820c5034e9d0
```

### Options

```
  -h, --help   help for join-status
```

### Options inherited from parent commands

```
      --authrpc-cert string   kwild's TLS certificate
      --output text           the format for command output - either text or `json` (default "text")
  -s, --rpcserver string      admin RPC server address (either unix or tcp) [default: unix:///tmp/kwil_admin.sock] (default "unix:///tmp/kwil_admin.sock")
  -S, --silence               Silence logs
      --tlscert string        kwil-admin's TLS certificate file for server to authenticate us [default: auth.cert] (default "auth.cert")
      --tlskey string         kwil-admin's TLS key file to establish a mTLS (authenticated) connection [default: auth.key] (default "auth.key")
```

