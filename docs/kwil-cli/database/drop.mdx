---
sidebar_position: 4
sidebar_label: "drop"
id: "drop"
title: "kwil-cli database drop"
description: "Drops a database from the connected network."
---

Drops a database from the connected network.

The drop coommand will drop a database schema, and all of its data, from the connected network.
This will only work if the wallet address that signs the transaction is the owner of the database.

Drop takes one argument: the name of the database to drop.

### Usage

```
kwil-cli database drop <db_name> [flags]
```

### Example

```
# Drop a database from a Kwil network.
	
$ kwil-cli database drop mydb
TxHash: a001d7ccf73b05d6aa0d749d25cec60fef4de606533ac21f48043c7cc65dfe1b
```

### Options

```
  -h, --help        help for drop
  -N, --nonce int   nonce override (-1 means request from server) (default -1)
```

### Options inherited from parent commands

```
      --chain-id string        The expected/intended Kwil Chain ID
      --kwil-provider string   The Kwil provider endpoint (default "127.0.0.1:50051")
      --output output format   the format for command output, either 'text' or 'json' (default text)
      --private-key string     The private key of the wallet that will be used for signing
      --tls-cert-file string   The path to the TLS certificate, this is required if the kwil provider endpoint is using TLS
```

