---
sidebar_position: 1
sidebar_label: "authenticate"
id: "kwil-cli-utils-authenticate"
title: "kwil-cli utils authenticate"
description: "Authenticate with a Kwil Gateway using a private key."
slug: /ref/kwil-cli/utils/authenticate
---

## kwil-cli utils authenticate

Authenticate with a Kwil Gateway using a private key.

### Synopsis

Authenticate with a Kwil Gateway using a private key.

The "authenticate" command will prompt you to sign a challenge from the Kwil Gateway. It
will store the returned auth cookie in "~/.kwil-cli/auth" for future use.

The Kwil CLI automatically handles authentication and re-authentication, however this tool
can be used to manually authenticate to a Kwil Gateway.


```
kwil-cli utils authenticate [flags]
```

### Examples

```
# Authenticate to a Kwil Gateway

kwil-cli utils authenticate
```

### Options

```
  -h, --help   help for authenticate
```

### Options inherited from parent commands

```
  -Y, --assume-yes             Assume yes for all prompts
      --chain-id string        the expected/intended Kwil Chain ID
      --kwil-provider string   the Kwil provider HTTP endpoint (default "http://localhost:8080")
      --output string          the format for command output - either 'text' or 'json' (default "text")
      --private-key string     the private key of the wallet that will be used for signing
  -S, --silence                Silence logs
      --tls-cert-file string   the path to the TLS certificate (if the provider endpoint is using TLS)
```

### SEE ALSO

* [kwil-cli utils](/docs/ref/kwil-cli/utils)	 - Various CLI utility commands.

