---
sidebar_position: 3
sidebar_label: "print-config"
id: "kwil-cli-utils-print-config"
title: "kwil-cli utils print-config"
description: "Print the current CLI configuration."
---

Print the current CLI configuration.

### Usage

```
kwil-cli utils print-config [flags]
```

### Options

```
  -h, --help   help for print-config
```

### Options inherited from parent commands

```
      --chain-id string        the expected/intended Kwil Chain ID
      --kwil-provider string   the Kwil provider gRPC endpoint (default "127.0.0.1:50051")
      --output text            the format for command output - either text or `json` (default "text")
      --private-key string     the private key of the wallet that will be used for signing
      --tls-cert-file string   the path to the TLS certificate (if the provider endpoint is using TLS)
```

